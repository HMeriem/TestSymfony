{% extends 'layout.html.twig' %}
{% block body %}
    <div class="alert alert-danger" id="fail-save" style="display: none;">L'enregistrement à échoué</div>
    <div class="col-md-8 col-md-push-2">
        {{ form_start(formCompetences) }}

        {{ form_label(formCompetences.intitule, 'Intitulé de la fonction', {'label_attr': {'style': 'color: grey;'}}) }}
            {{ form_row(formCompetences.intitule, {'attr': {'style': 'color: grey;'}}, {'label_attr': {'style': 'color: grey;'}}) }}
            {{ form_row(formCompetences.niveau, {'attr': {'style': 'color: grey;'}}) }}
            {{ form_row(formCompetences.categorie, {'attr': {'style': 'color: grey;'}}) }}
            {{ form_rest(formCompetences) }}
        {{ form_end(formCompetences) }}
    </div>
    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    ...
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    $('form[name="competences"').submit(function(event){
        //event.preventDefault();
        /*$.post('{{ path('competences') }}',
            $('form[name="competences"').serialize(),
            function(res){
                if(res) {
                    $('#exampleModal').modal();
                } else {
                    $('#fail-save').fadeIn();
                    window.setTimeout(function(){
                        $('#fail-save').fadeOut();
                    }, 5000);
                }
        })*/
    })
{% endblock %}